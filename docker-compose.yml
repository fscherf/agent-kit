services:
  python:
    build:
      context: .
      dockerfile: python.Dockerfile

    hostname: agent-kit

    volumes:
      - ./:/app

    user: 1000:1000
    working_dir: /app
    command: /app/entrypoint.sh
